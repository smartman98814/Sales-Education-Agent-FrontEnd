'use client';

/**
 * Web3/Wagmi support is disabled per user request.
 * This file provides a stub config to keep the app stable
 * without pulling in blockchain providers.
 */

export const wagmiConfig = {} as any;
export const chain = {} as any;
